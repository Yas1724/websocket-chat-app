# websocket-chat-app
This is a real-time chat application built using WebSockets, where:Messages are exchanged bi-directionally  Users can join rooms  Messages are broadcast only within the same room  Frontend is built using React + TypeScript  Backend is built using Node.js + ws. This project focuses on core real-time systems concepts, not UI gimmicks.
